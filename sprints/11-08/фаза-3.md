# Фаза 3 «Функционал» — промежуточный отчёт

**Спринт:** 11-08 ([sprint-plan.md](./sprint-plan.md)) · предыдущий этап — [фаза-2.md](./фаза-2.md)
**Дата:** 11.08.2026
**Задачи фазы:** T11–T20
**Статус:** 🟡 в работе. Закрыты T11, T17, T18. **T13 прерван на середине**, остальное не начато.

---

## 1. Статус задач

| Задача | Исполнитель | Статус |
|---|---|---|
| **T11** Links CRUD + CodeGeneratorService | backend-агент (продолжение T5) | ✅ проверено вживую |
| **T12** Redirect + учёт кликов | — | ❌ не начат (был заблокирован `app.module.ts`, сейчас разблокирован) |
| **T13** Stats-эндпоинты | backend-агент | ⛔ **прерван**: агент упал по лимиту сессии, успел создать только пустые директории `src/stats/dto/` |
| **T14–T16** api-тесты | — | ❌ не начаты (каркас готов с фазы 2) |
| **T17** LinksPage + CreateLinkPage | frontend-агент (продолжение T6) | ✅ проверено |
| **T18** DashboardPage | frontend-агент | ✅ проверено |
| **T19** LinkDetailsPage | — | ❌ не начат |
| **T20** Frontend unit-тесты | — | ❌ не начат |

Обе задачи шли **продолжением уже существующих агентов** через `SendMessage`, а не новым холодным запуском — агенты сохранили контекст T5 и T6, промпты содержали только дельту.

---

## 2. Сбой backend-агента и его последствия

Агент прервался на переходе от T11 к T13 с ошибкой лимита сессии. Само по себе это не страшно — T11 он закончил. Проблема в том, **какое состояние он оставил**.

**Что было сломано.** Агент прописал `StatsModule` в `app.module.ts` *до* того, как написал сам модуль. После его падения дерево не компилировалось: `Cannot find module './stats/stats.module'`.

**Первый слой починки.** Убрана строка импорта из `app.module.ts` — компиляция восстановлена.

**Второй слой, более серьёзный.** После этого приложение перестало стартовать уже в рантайме:

```
TypeORMError: Entity metadata for Link#clickEvents was not found.
```

`Link` содержит `@OneToMany` на `ClickEvent`, но `autoLoadEntities: true` подхватывает только те сущности, которые кто-то зарегистрировал через `TypeOrmModule.forFeature([...])`. Регистрировать `ClickEvent` должен был как раз ненаписанный `StatsModule`. То есть связь между сущностями оказалась завязана на порядок написания фиче-модулей.

**Решение.** В `database.module.ts` `autoLoadEntities: true` заменён на глоб `entities: [__dirname + '/../**/*.entity{.ts,.js}']`. Метаданные больше не зависят от того, какие модули уже написаны, и это совпадает с тем, как устроен `data-source.ts` для CLI. Причина зафиксирована комментарием в коде.

**Урок для оставшихся задач:** не подключать модуль в `app.module.ts` раньше, чем он написан. Промежуточное состояние агента может стать конечным в любой момент.

---

## 3. Проверки

### T11 — живая проверка против поднятого стека

| Кейс | Результат |
|---|---|
| `POST /api/links` | ✅ 201, `code` длиной 7, `shortUrl` собран из `BASE_URL` |
| `POST` с занятым `customCode` | ✅ 409 `CODE_TAKEN` |
| `POST` с `originalUrl: "not-a-url"` | ✅ 400 `VALIDATION_ERROR` + `details` |
| `GET /api/links?limit=2` | ✅ 200, `items`/`page`/`limit`/`total`, сортировка по `created_at DESC` |
| `GET /api/links/999` | ✅ 404 `LINK_NOT_FOUND` |
| `GET /api/links/abc` | ✅ 400 `VALIDATION_ERROR` |
| `PATCH /api/links/1` | ✅ 200 |
| `DELETE /api/links/2` | ✅ 200 `{ deleted: true }` |

Все ответы — в конверте `{ data, error }` с соблюдением инварианта.

### Сборка и тесты

| Проверка | Результат |
|---|---|
| backend `npm run typecheck` | ✅ 0 ошибок |
| backend `npm test` | ✅ **32 теста** (было 13 после фазы 1) |
| frontend `npm run typecheck` | ✅ 0 ошибок |
| frontend `npm run build` | ✅ собирается |
| frontend `npm test` | ✅ 10 тестов |
| msw не утёк в бандл | ✅ `grep msw dist/assets/*.js` пусто |
| `react-router-dom` не используется | ✅ пусто |
| `make start` после починки | ✅ все контейнеры `healthy` |
| `make test-api` на текущем коде | ✅ 7/7 |

---

## 4. Решения фронтенд-агента, требующие внимания

1. **`ToastProvider` подключён в `App.tsx`, а не в `main.tsx`.** Причина: `routing.spec.tsx` рендерит `<App/>` напрямую, минуя `main.tsx`, а `LinkForm` безусловно вызывает `useToast()` — тест падал бы без провайдера. Решение корректное, но расходится с `docs/design/ui-kit.md` — документацию стоит поправить.
2. **`ClicksChart` спроектирован под переиспользование в T19**: пропсы `data / isLoading / isError / onRetry / range / onRangeChange / metrics`, не завязан на конкретный хук. T19 подключает `useLinkDailyStats` и передаёт `metrics={["clicks", "uniqueVisitors"]}`.
3. **Клик по строке**: в `LinksTable` кликабелен только код ссылки (внутри строки есть `CopyButton` и toggle — клик по всей строке конфликтовал бы), в `TopLinksTable` — вся строка, как требует контракт.
4. **`ActiveToggleCell` реализован локально** в `LinksTable.tsx`, компонента `Toggle` в дизайн-системе нет. T19 под `link-header-active-toggle` либо продублирует паттерн, либо вынесет его в `components/links/`.
5. **Размер бандла 697 КБ / 205 КБ gzip** из-за recharts. Не оптимизировано, code-splitting по роутам не делался — кандидат в `tech-debt.md`.

---

## 5. Открытые вопросы

| # | Вопрос | Кому |
|---|---|---|
| 1 | Код ошибки для несуществующего маршрута: сейчас `INTERNAL_ERROR` с HTTP 404, что противоречит `error-codes.md`. **Тянется с фазы 1, не закрыт.** Тест `API-03` намеренно ослаблен до его решения | координатор |
| 2 | `VITE_API_URL` не резолвится изнутри контейнера Playwright — блокирует T22–T24 | до старта T10 |
| 3 | Расхождение ui-kit.md с фактическим местом подключения `ToastProvider` | T19 или T26 |
| 4 | Bundle 697 КБ, code-splitting не делался | T28 / tech-debt |
| 5 | Compose вырос до 6 сервисов — не отражено в `docs/plans/linkboard.md` §6.1 | T26 |

---

## 6. Что дальше

Ближайшие незакрытые задачи в порядке приоритета:

1. **T13** (stats-эндпоинты) — единственная задача фазы, оставшаяся от прерванного агента. На критическом пути.
2. **T12** (редирект) — разблокирован, `app.module.ts` свободен. Небольшой, но тоже на критическом пути.
3. **T14–T16** — api-тесты можно писать сразу после появления эндпоинтов, каркас готов.
4. **T19, T20** — фронтенд, зависят от T17/T18, которые закрыты.
5. **T10** — каркас e2e, требует предварительного решения по `VITE_API_URL`.

Практическое ограничение: лимит сессии уже был исчерпан один раз (сброс в 14:00 МСК), это стоит учитывать при планировании делегирования агентам.
